<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- High Contrast Colors -->
    <SolidColorBrush x:Key="PrimaryBackground" Color="#000000"/>
    <SolidColorBrush x:Key="PrimaryForeground" Color="#FFFF00"/>
    <SolidColorBrush x:Key="ButtonBackground" Color="#000000"/>
    <SolidColorBrush x:Key="ButtonForeground" Color="#FFFF00"/>
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#333333"/>
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="#666666"/>
    <SolidColorBrush x:Key="TextBoxBackground" Color="#000000"/>
    <SolidColorBrush x:Key="TextBoxForeground" Color="#FFFF00"/>
    <SolidColorBrush x:Key="TextBoxBorder" Color="#FFFF00"/>
    
    <!-- Global Font Size -->
    <System:Double x:Key="GlobalFontSize" xmlns:System="clr-namespace:System;assembly=mscorlib">16.0</System:Double>
    
    <!-- Button Style -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonForeground}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonForeground}"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonPressedBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- TextBox Style -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource TextBoxBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBoxForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorder}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    
    <!-- Window Style -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource PrimaryBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForeground}"/>
    </Style>
    
    <!-- Label Style -->
    <Style TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForeground}"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    
    <!-- TextBlock Style -->
    <Style TargetType="TextBlock">
        <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForeground}"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    
</ResourceDictionary>
